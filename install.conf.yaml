- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.gitconfig: gitconfig
- create:
    - ~/downloads

#- shell:
#    - chsh -s $(which zsh) # fails due to needing password
